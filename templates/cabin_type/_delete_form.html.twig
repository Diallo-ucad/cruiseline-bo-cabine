<form method="post" action="{{ path('app_cabin_type_delete', {'id': cabin_type.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cabin_type.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
