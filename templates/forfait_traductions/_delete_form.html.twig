<form method="post" action="{{ path('app_forfait_traductions_delete', {'id': forfait_traduction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forfait_traduction.id) }}">
    <button class="btn">Delete</button>
</form>
