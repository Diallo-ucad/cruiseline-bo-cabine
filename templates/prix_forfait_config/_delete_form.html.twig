<form method="post" action="{{ path('app_prix_forfait_config_delete', {'id': prix_forfait_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prix_forfait_config.id) }}">
    <button class="btn">Delete</button>
</form>
