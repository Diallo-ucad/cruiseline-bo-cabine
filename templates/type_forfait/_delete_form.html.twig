<form method="post" action="{{ path('app_type_forfait_delete', {'id': type_forfait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_forfait.id) }}">
    <button class="btn btn-danger btn-block">Supprimer</button>
</form>
