<form method="post" action="{{ path('app_forfait_content_delete', {'id': forfait_content.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forfait_content.id) }}">
     <button class="btn btn-danger btn-sm btnDelete" aria-label="Left Align" data-toggle="tooltip" data-placement="right" title="Supprimer le contenu">
        <i class="bi bi-trash"></i>
    </button>
</form>
