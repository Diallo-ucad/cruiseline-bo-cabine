<form method="post" action="{{ path('app_forfait_type_traductions_delete', {'id': forfait_type_traduction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forfait_type_traduction.id) }}">
    <button class="btn">Delete</button>
</form>
